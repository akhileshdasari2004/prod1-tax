---
interface Props {
  label: string;
  valueId: string;
  prominent?: boolean;
}

const { label, valueId, prominent = false } = Astro.props;
---

<div>
  <p class="results-label">{label}</p>
  <p id={valueId} class:list={[prominent ? 'results-value mt-1' : 'text-title-4 mt-1']}>—</p>
</div>
