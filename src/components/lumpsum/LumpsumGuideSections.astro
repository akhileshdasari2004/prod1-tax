---
import {
  lumpsumFormula,
  lumpsumExample,
  lumpsumBenefits,
  lumpsumGuideSections,
} from '../../content/lumpsum-calculator-page';
---

<div class="mt-24 max-w-3xl">
  <section id="formula" class="scroll-mt-28">
    <h2 class="text-title-2 text-balance">{lumpsumFormula.title}</h2>
    <p class="text-body mt-4">{lumpsumFormula.summary}</p>
    <div
      class="mt-6 rounded-2xl border px-6 py-5 font-mono text-body-sm"
      style="border-color: var(--border-subtle); background: var(--bg-secondary); color: var(--text-primary);"
    >
      {lumpsumFormula.formula}
    </div>
    <ul class="mt-6 list-none space-y-3 p-0">
      {
        lumpsumFormula.definitions.map((row) => (
          <li class="flex gap-3 text-body-sm">
            <span class="shrink-0 font-semibold" style="color: var(--text-primary);">{row.symbol}</span>
            <span>{row.meaning}</span>
          </li>
        ))
      }
    </ul>
    <ul class="mt-6 list-disc space-y-2 pl-6 text-body">
      {lumpsumFormula.notes.map((note) => (
        <li>{note}</li>
      ))}
    </ul>
  </section>

  <section id="example" class="mt-20 scroll-mt-28">
    <h2 class="text-title-2 text-balance">{lumpsumExample.title}</h2>
    <p class="text-body mt-4">
      Suppose you invest <strong>${lumpsumExample.inputs.principal.toLocaleString()}</strong> once at{' '}
      <strong>{lumpsumExample.inputs.rate}%</strong> expected annual return for{' '}
      <strong>{lumpsumExample.inputs.years} years</strong>:
    </p>
    <ol class="mt-6 list-decimal space-y-3 pl-6 text-body">
      {lumpsumExample.steps.map((step) => (
        <li>{step}</li>
      ))}
    </ol>
    <p class="text-caption mt-6">{lumpsumExample.disclaimer}</p>
  </section>

  <section id="benefits" class="mt-20 scroll-mt-28">
    <h2 class="text-title-2 text-balance">{lumpsumBenefits.title}</h2>
    <ul class="mt-8 space-y-8">
      {
        lumpsumBenefits.items.map((item) => (
          <li>
            <h3 class="text-title-4">{item.heading}</h3>
            <p class="text-body mt-2">{item.body}</p>
          </li>
        ))
      }
    </ul>
  </section>

  {
    lumpsumGuideSections.map((section) => (
      <section id={section.id} class="mt-16 scroll-mt-28">
        <h2 class="text-title-3 text-balance">{section.heading}</h2>
        {section.paragraphs.map((p) => (
          <p class="text-body mt-4">{p}</p>
        ))}
      </section>
    ))
  }
</div>
